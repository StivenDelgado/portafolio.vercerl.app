version: '3.8'
services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - .:/app
    command: npm run dev
    networks:
      - portfolio_net
    restart: always
networks:
  portfolio_net:
    name: portfolio_net
    driver: bridge